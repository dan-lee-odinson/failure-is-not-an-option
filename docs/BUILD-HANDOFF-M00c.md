# FNO-M00c — Playtest-2 presentation fixes: Build handoff

**Task:** FNO-M00c · **From:** Claude (Claude Code, Fable 5.1) on artemis · **To:** Dan (director) and Codex · **Date:** 7 September 2026 · **Status:** DONE. All ten changes in the handoff are built and checked. Content stays **0.5.1**; no content, schema-behaviour, engine or save change (the one schema edit is the no-op `hint` field the handoff asked for).

## 1. Where it is

| Item | Value |
|---|---|
| Repository | `C:\Users\wolfe\projects\failure-is-not-an-option`, `main`, from `223a6cb` (M00b delivery) |
| Delivery commit | *(filled in the zip copy)* |
| Content | **0.5.1**, fingerprint `9056d94a25e9691c2245c89647aabcc6e3ae994d2af482f62232560d472c98f6` (unchanged) |
| Validate | 56 complete routes, 6 outcomes, 112 plan commits, 0 draws (unchanged) |
| Unit tests | `npm test`: **108 tests across 12 files, all passing** |
| Fail-on-purpose | 9 planted faults, 9 detected |
| Browser tests | `npm run test:e2e`: **22 tests, all passing (Chromium; 89 screenshots at 1920×1080 and 1366×768, default and enlarged text)** |
| Dialogue sheet | regenerated: **627 strings (203 branch-only) from 56 routes; content 292 · core 73 · app 262 — the 35 rows History no longer renders are kept as `history-hidden`; the app-authored count fell because the History lists and the prompt-line readout are gone** |

Commands Dan runs: `npm install` · `npm run dev` → http://localhost:5173 · `npm test` · `npm run validate` · `npm run placeholders` · (`npm run test:e2e`, `npm run dialogue-sheet`).

**Note on 0.5.2.** `FNO-M00-Codex-Content-v0.5.2.zip` was already in the shared folder when this pass started. The handoff and Dan's instruction both fix content at 0.5.1 for M00c, so 0.5.2 was not opened or integrated; it lands as its own content-only step (the `hint` field below is ready for it).

## 2. The changes, by the handoff's numbers

1. **Sound on by default (note 1).** Begin (or Skip) turns the master on unless a sound setting is already persisted (`fno.audio`); the SOUND key and the volumes are unchanged, and turning it off persists. Before Begin the page is silent and no AudioContext exists. The e2e case is now "silent until Begin, on after Begin, a persisted off stays off"; the replay-identity and no-audio-in-the-log checks stay.
2. **One continuous opening scroll (notes 2, 3, 4).** The dedication and the notices are one column with a chapter gap of about 1.5 line-heights; no Continue between them; the scrollbar is hidden (`scrollbar-width: none`, WebKit rule) while wheel and keyboard scrolling still work when paused. When the last line has cleared the top: 1.2 s hold → the prose fades to black over 1.0 s → the hero title fades in over 1.5 s. Continue at any time: 0.4 s fade out, 0.4 s fade in. Pause/Resume, Skip and Sound stay outside the text. Reduced motion: two static pages (dedication, notices) with Continue and immediate cuts. The montage slot stays where it was; the `notices` stage is now visited only under reduced motion. The fade steps are `setTimeout`s (fake-timer testable) and the visual is CSS (`fade-out` transition, `op-fade-in` animation), so the reduced-motion rule cuts them.
3. **Hero title 25 % smaller (note 5).** `app/title-layout.json` study `a` at 0.75: FAILURE 240.594 px, IS NOT AN 202.05 px, OPTION 270.608 px; left edge x 475; baselines scaled about the block's vertical centre (y 500.2, from the cap top of FAILURE to the OPTION baseline; cap height 0.7 em): 376.3 / 557.1 / 792.6. The v003 values are kept beside them as `source_*` and a unit test checks the arithmetic. The menu lockup keeps its 0.826 ratio. Glen's clear zone and the ≥ 18 px Continue label are re-asserted in e2e.
4. **Full-screen line and key (note 6).** Under the menu keys, in the subtitle style: "Best played full screen — press F11 on Windows." A FULL SCREEN key (selector face) calls `document.documentElement.requestFullscreen()`; it is hidden when `document.fullscreenEnabled` is false or the request is refused, and reads EXIT FULL SCREEN (selected face) while active (`fullscreenchange` repaints). Keyboard-reachable like every key.
5. **Crisis cue → Drum Background (note 9).** `Mission in Danger Drum Background.mp3` copied unchanged to `public/audio/` with a manifest entry (title, credit and rights from the audio README; measured duration 126.854 s; −17.1 LUFS per the README). `app/music-map.json`: the `crisis` cue's asset is `audio-mission-in-danger-drum-background` with `gain: 0.75` (a new per-cue level the director applies on top of the music bus), start 0.0, once, 2 s fade on the return Choose, still provisional. *Mission in Danger* keeps its manifest entry and sits in `reserved`, so it stays in the About OST list. Unit tests cover the asset, the gain and the file; README has a cue table.
6. **Emblem beneath the UI layer (note 10).** The plate and the emblem now live in one `room-layer` (fixed, z-index 0) and every UI region (status bar, stage, evidence, strip; the hero overlay) sits above it (z-index 1 and 2). Previously the emblem carried its own z-index inside the console's stacking context and painted over the strip. The e2e emblem assertion now checks the emblem is inside the room layer, that the layer's z-index is below every region's, and that the element at the emblem's centre is not the emblem whenever a panel covers it.
7. **Details open by default (note 12).** Every card's Details starts open, including the committed cards on the receipt; the per-card toggle memory stays. The return-fork special case is gone.
8. **History panel trim (note 14).** History renders the history explanation, the lamp sentence and the sources H1–H8, and nothing else: the per-item provenance lines (reports, procedures, departures), the fiction register, the people list and the mission anchors are not rendered. The data is untouched. **Dialogue sheet:** those strings stay on the sheet with kind `history-hidden` (the per-report provenance rows, previously kind `evidence.provenance`, are now `history-hidden` too, since nothing renders them); the generator adds them as content strings owned by the History bucket, and the reachability test checks every `history-hidden` row against `historyHiddenStrings()` in both directions, so the proof stays honest. README's rights and sources sections are unchanged.
9. **Conversation panel.** `overflow-x: hidden` on the panel and body; every text block is `max-width: min(62ch, 100%)` (the old fixed 62ch overflowed at enlarged text beside a portrait); the portrait column is `minmax(0, auto)`. The panel is a flex column: head, a scrolling body, and Glen's questions pinned in a footer, so the questions are in view without scrolling at both viewports and both text sizes; the strip's max height now leaves the stage room for them. The rehearsal readout appears once, under "Supported by" on each card (Details open); the prompt-line copy is gone and only the caution sentence stays.
10. **Idle highlight and hints (note 8, presentation half).** A 30 s idle timer, presentation only: pointer, key, wheel and input events reset it; on expiry on a non-decision screen the primary continuation key (the `data-focus-default` key: Begin, Continue, the hero Continue, NEW CAMPAIGN, the console Continue, the debrief's Continue) takes the kit selected face at 0.85 opacity, steady. On a decision screen — any screen with a choice: the decisions, the preparation screen (its Finish key included) and the plan screen before Commit — nothing is highlighted; if the node carries `hint` text it is shown in a paper strip above the cards, and no content carries one yet. Settings has **HINTS: SHOW / HIDE** (default show, persisted `fno.hints`). Under reduced motion nothing appears. Not under an overlay. `hint?: string` is on the decision-node type and in `schema/mission.schema.json` as a no-op read (the validator accepts it; a unit test plants one on a raw copy and validates, and proves the engine log is unchanged with and without it) so 0.5.2 drops in as content only.

## 3. Checks

- `npm run validate`: unchanged numbers and fingerprint.
- `npm test`: 108 tests across 12 files, all passing — new file `tests/core/presentation-m00c.test.ts` (opening column and reduced-motion pages, fade classes, the 0.75 title arithmetic, full-screen key states, the room layer, Details and the panel footer, the History trim, idle and hints, the hint strip with a planted hint); updated expectations in the M00b, sheet and audio tests.
- `npm run test:e2e`: 22 tests, all passing (Chromium; 89 screenshots at 1920×1080 and 1366×768, default and enlarged text) — updated: opening flow (one column, hidden scrollbar, three scroll points, Continue's quick fade), reduced motion (two pages, cuts), the scroll's end (hold → fade → title), audio default, History, Details; added: fade timing under fake timers (`page.clock`: 1.2 s / 1.0 s / 1.5 s and 0.4 s / 0.4 s), the FULL SCREEN key absent when the API is unavailable, the emblem beneath the panels (in every room shot), no horizontal overflow and questions in view in the conversation panel at both viewports and sizes, the idle highlight after 30 s with fake timers and its absence under reduced motion, the HINTS toggle persisted.
- Contrast: re-measured at every screenshot point; the menu line, the full-screen key, the idle-highlighted key and the panel footer are all in the measured set. At the 89 screenshot points: 646 text samples (worst 5.38:1, `.menu-subtitle` on `1920x1080-default/04-menu-continue-disabled`) and 931 rendered controls (worst 5.58:1, `select-g9-plan-systems-contact [disabled]` on `1366x768-large/21-gemini-9a-planning`); the full-screen line reads 5.38:1 at worst, the FULL SCREEN key 6.93:1, and the idle-highlighted key 9.4:1 (the selected face at 0.85 opacity keeps its dark ink). The face × ink table is unchanged from M00b (minimum 5.38:1). The hint strip is paper on paper ink (13:1) and appears only once content carries a hint. The tightest layout is enlarged text at 1366×768 on the return decision: the panel shows the speaker's portrait above Glen's two questions and the lines scroll beneath it; the questions never leave view.
- Screenshots (in the zip): the opening at three points of the scroll (`01`, `01b`, `01c`), the hero title (`03`), the menu (`04`, `06`), the History panel (`07`), the return decision (`15`), the order receipt (`16`), and the idle highlight (`22`), at 1920×1080 and 1366×768, default and enlarged text (the idle shot at 1920×1080 default).

## 4. Decisions in this pass that Dan may reverse

- The idle highlight applies to whichever key is the screen's primary continuation, on the opening and menu too (Begin, Continue, NEW CAMPAIGN), not only in play. The strength is one CSS variable (`--idle-opacity: 0.85`).
- "A decision screen with the idle highlight" in the screenshot list cannot exist yet: a decision highlights nothing until a `hint` arrives, so the idle screenshot is the mission brief's Continue key.
- On the return decision the caution sentence ("Unrehearsed does not mean untrained or incapable. Both orders remain available.") stays under Glen's question; only the readout row was dropped.
- The History panel keeps only the three items the handoff named; the people list and the mission anchors went with the register (they are on the sheet as `history-hidden` and in the README).

## 5. Open questions

- Playtest 3 decides the crisis cue by ear (start offset and the 0.75 level are numbers in the map).
- When 0.5.2 arrives, its `hint` strings need no code: they render in the strip after 30 s of idling on that decision.
