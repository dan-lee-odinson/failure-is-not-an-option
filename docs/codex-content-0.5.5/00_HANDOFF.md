# Content 0.5.5 — encounter-based evidence and reference pages

For Dan and Claude · 8 September 2026 · Response 50 · Based on doc 49 notes 6–7 and binding R2.

**Baseline:** integrated 0.5.4 at `423e1ec006315b1fdc3ec8b96d9fc00525b32490` (HOTFIX-01; includes the in-force licence changes after 544fc65).
**Candidate:** 0.5.5 · fingerprint `4c22b8e7949ba84df3799d692dcec360391919b5b18936e9c141991883efff2d`.

This packet supplies the content half of R2: explicit unlocks on all 16 evidence records and all three procedure/task records, the required schema/type/validator changes, the Binder empty-state line, the approved H5 note, unchanged in-force licence strings, and review/tests/patch files. **Claude Code still implements encounter-based visibility and visible addition announcements. Applying this packet alone does not fix the live spoiler bugs.** The original repository and live site were not changed.

## Scope and the four presenting additions

Six of the sixteen records in evidence.json are reference pages: three preparation worksheets, the network primer, the termination rule, and the later post-flight context. The four pages seen in Dan's screenshot are not the complete authored inventory. procedures.json contains two adopted standing procedures and one commissioned task. Both collections now require unlocked_by; the latter also retains its existing adoption requirement.

No evidence items were cut. Under the request's explicit allowance for items not presented by the script, four narration lines were added: g8-line-recovery-readback, g8-line-systems-readback, g8-line-contact-readback and g8-line-postflight-context. Each copies its existing evidence body verbatim, with speaker null. The three readbacks retain their preparation facts as line conditions. There is no new historical claim, CAPCOM speech or revised existing dialogue. Ten stable line ids identify six existing lines/answers and these four additions. Source attribution, H7 page metadata, all options, effects, costs, branch conditions, outcomes and follow-on content are preserved.

Thirteen evidence provenance notes now describe encounter timing instead of stale automatic acquisition timing. In particular, post-flight context is available only after its narration in the later accountability brief. Its H5 provenance remains a secondary-account paraphrase; it is not relabeled as a transcript. Existing evidence kind, simulated flag, observation status and body are unchanged.

**No {start:true} exemptions.** None of these cards needs advance exposure: the worksheets depend on selected rehearsals, the primer is introduced by loss of contact, the rule reveals the crisis, and every remaining record describes an event, report or lesson encountered later. General flight-director competence in the opening brief does not justify exposing those cards.

## Complete item audit

The JSON trigger is an additional presentation gate, not a new acquisition effect. For line triggers, actually display the eligible line or asked answer; owning-node entry alone is insufficient. The same table is supplied as Markdown, CSV and JSON in docs/unlock-review-055.*.

| ID | unlocked_by | Presenting line / scene | Why |
|---|---|---|---|
| `g8-ev-rule` | `{"line":"g8-line-rcs-termination"}` | `g8-stabilization-report.lines[0]` — They've regained control using the reentry thrusters. They've used substantial propellant from both RCS rings. That means we terminate the mission. | Mara explicitly reports both RCS rings, substantial use and mission termination. Earlier briefing acquisition is internal only; do not expose the title. The earlier optional crew relay does not unlock this card. |
| `g8-ev-contact-primer` | `{"node":"g8-loss-of-contact"}` | `g8-loss-of-contact.text` — The scheduled tracking pass has ended. There's no new report from the spacecraft. | The scheduled pass has ended and no new report is available. R2 explicitly selects this loss-of-contact scene; not the briefing or preparation option that cites the primer. |
| `g8-ev-contact-worksheet` | `{"preparation":"g8-prep-contact"}` | `g8-prep-select.text + selected option g8-prep-contact` — In the contact drill, Houston CAPCOM and a tracking-ship CAPCOM practice sending an instruction, reading it back, and confirming receipt to FLIGHT. In the recovery drill, Elias and the Department of Defense recovery coordinator practice passing the aircraft's location report to the ship and confirming who sends the pickup instruction. In the systems drill, Mara calls a low-propellant warning to FLIGHT and CAPCOM together, then passes her recommended response before the next entry task begins. Selected: Rehearse sending an instruction and confirming its receipt. | Only the selected contact rehearsal produces this worksheet. Reading the menu is not selecting the drill. |
| `g8-ev-recovery-worksheet` | `{"preparation":"g8-prep-recovery"}` | `g8-prep-select.text + selected option g8-prep-recovery` — In the contact drill, Houston CAPCOM and a tracking-ship CAPCOM practice sending an instruction, reading it back, and confirming receipt to FLIGHT. In the recovery drill, Elias and the Department of Defense recovery coordinator practice passing the aircraft's location report to the ship and confirming who sends the pickup instruction. In the systems drill, Mara calls a low-propellant warning to FLIGHT and CAPCOM together, then passes her recommended response before the next entry task begins. Selected: Rehearse passing the pickup instruction from the recovery desk to the ship. | Apply the same selected-rehearsal rule to the third worksheet; it must never appear on a run without recovery preparation. |
| `g8-ev-systems-worksheet` | `{"preparation":"g8-prep-systems"}` | `g8-prep-select.text + selected option g8-prep-systems` — In the contact drill, Houston CAPCOM and a tracking-ship CAPCOM practice sending an instruction, reading it back, and confirming receipt to FLIGHT. In the recovery drill, Elias and the Department of Defense recovery coordinator practice passing the aircraft's location report to the ship and confirming who sends the pickup instruction. In the systems drill, Mara calls a low-propellant warning to FLIGHT and CAPCOM together, then passes her recommended response before the next entry task begins. Selected: Rehearse calling a low RCS propellant warning to FLIGHT and CAPCOM. | Only the selected systems rehearsal produces this worksheet. Its propellant-warning title is hidden before adoption. |
| `g8-ev-docked` | `{"line":"g8-line-docking-confirmation"}` | `g8-docking-report.lines[0]` — Flight, Rose Knot Victor relays Armstrong's report: they're docked. | First presented docking confirmation through Lovell. Keep crew attribution and H7 provenance unchanged. |
| `g8-ev-crisis` | `{"line":"g8-line-crisis-relay"}` | `g8-crisis-report.lines[0]` — Flight, Coastal Sentry Quebec relays Scott: “We have serious problems here. We’re … tumbling end over end up here.” They’re disengaged from the Agena. | First emergency relay through Lovell, attributed to Scott via Coastal Sentry Quebec. Not during the contact gap. |
| `g8-ev-stabilized` | `{"line":"g8-line-rcs-termination"}` | `g8-stabilization-report.lines[0]` — They've regained control using the reentry thrusters. They've used substantial propellant from both RCS rings. That means we terminate the mission. | Mara now reports control regained and the use of the reentry thrusters; pair the receipt with that report. |
| `g8-ev-return` | `{"line":"g8-line-return-opportunities"}` | `g8-return-brief.lines[0]` — Two return opportunities. The first gets them down sooner. The second puts the recovery force closer. We can't have both. | Elias now describes the two simulated return opportunities. The card summarizes this planning tradeoff. |
| `g8-ev-air` | `{"line":"g8-line-recovery-risk-answer"}` | `g8-return-brief.questions[g8-q-recovery-risk].answer` — The nearest ship is hours away. They'll be waiting in the capsule, in darkness, with the sea wearing them down. Air support helps, but it doesn't make the ship any closer. | The earlier automatic remark says only that ocean time is risky. The optional answer actually explains the ship delay and the limit of air support. If never asked, this card stays hidden even post-flight. |
| `g8-ev-reserve` | `{"line":"g8-line-reserve-risk-answer"}` | `g8-return-brief.questions[g8-q-reserve-risk].answer` — They'll spend another orbit in a damaged spacecraft. RCS propellant is already low; they need it to control their attitude during entry. If the warning or our instruction arrives late, they'll have less time to act. | The earlier automatic remark says only that the crew might not stay stable. This optional answer explains the damaged spacecraft, low propellant and warning timing. If never asked, the card stays hidden. |
| `g8-ev-recovery-readback` | `{"line":"g8-line-recovery-readback"}` | `g8-return-brief.lines[3]` — Elias and the DoD coordinator have agreed who sends the final pickup instruction to the ship. The readback is complete. This should prevent confusion during the approach, but the ship still has to reach the capsule. | Added presenting narration, copied verbatim from this evidence body, conditional on g8-prepared-recovery. Arrival at return planning alone is insufficient. |
| `g8-ev-systems-readback` | `{"line":"g8-line-systems-readback"}` | `g8-return-brief.lines[4]` — Mara has checked the call with FLIGHT and CAPCOM. Both desks know to expect the RCS warning and her recommendation together, so CAPCOM can pass the instruction without waiting for the assessment to be repeated. | Added presenting narration, copied verbatim from this evidence body, conditional on g8-prepared-systems. No preparation means no readback line or card. |
| `g8-ev-contact-readback` | `{"line":"g8-line-contact-readback"}` | `g8-return-brief.lines[5]` — CAPCOM has identified who sends the next instruction and who confirms its receipt. Until that confirmation comes back, FLIGHT will treat the instruction as pending. The same check applies to either return. | Added presenting narration, copied verbatim from this evidence body, conditional on g8-prepared-contact. No preparation means no readback line or card. |
| `g8-ev-recovered` | `{"node":"g8-pickup-report"}` | `g8-pickup-report / both resolution effect labels` — Both crew members recovered alive | Both branch resolutions display this recovery fact at the pickup report. Do not use the earlier splashdown as the trigger. |
| `g8-ev-postflight-context` | `{"line":"g8-line-postflight-context"}` | `g8-accountability-brief.lines[0]` — Post-flight analysis has cleared Armstrong and Scott of blame. Walt Cunningham has criticized aspects of their performance; Tom Stafford has argued that undocking from Agena was the wrong decision. Gene Kranz has identified a failure on the ground: too few contingency procedures for the docked phase. Frank Borman and Wally Schirra have defended the crew. | Added presenting narration, copied verbatim from this card. The old brief only said there was disagreement; it did not present all five attributed positions. This stays in the later accountability scene. H5 is a secondary account, not an air-ground transcript. |
| `proc-report-provenance` | `{"node":"g8-lesson-decision"}` | `g8-lesson-decision.options[g8-adopt-provenance]` — Make every report say what's been confirmed and what's still unknown. Give the room a clear record of what was known and when. | The lesson option introduces this practice. Binder display also requires existing ledger adoption via g8-adopt-provenance; merely entering the decision does not add either procedure. |
| `proc-recovery-crosscheck` | `{"node":"g8-lesson-decision"}` | `g8-lesson-decision.options[g8-adopt-recovery]` — Put aircraft support, ship arrival, and confirmation of each on one recovery sheet. Let the recovery desk check available help against the time the crew will spend at sea. | The lesson option introduces this practice. Binder display also requires existing ledger adoption via g8-adopt-recovery; the unchosen procedure stays hidden. |
| `proc-docked-contingencies` | `{"node":"g8-accountability-response"}` | `g8-accountability-response / g8-response-ground-accountability.text` — You accept responsibility for the missing ground procedures. Armstrong and Scott's confidence in you strengthens. The critics haven't withdrawn their objections, but the ground team begins work on docked-flight contingencies. | Only the ground-accountability response commissions this work. Require ledger adoption from g8-response-ground-accountability as well as the node encounter. Crew-blame response never adds it. It remains a commissioned task, not a finished flight rule. |

## Availability contract for Claude Code

1. Derive encounters from the existing run inputs/state and their replayed presentation boundaries. Do not add a mechanics input, random draw or domain log entry. Node means the scene was presented; line means that line was displayed under its condition, including an optional question actually being asked. For an event line, only the selected resolution qualifies. Preparation means that option was successfully chosen, not merely shown.
2. Evidence visibility is acquisition AND encounter unlock AND existing visible_when, if present. Procedure visibility is ledger adoption AND encounter unlock. The commissioned task remains a task. Never expose unchosen procedures simply because the lesson node was visited.
3. Preserve the original acquisition effects/documents. The rule and network primer are still acquired internally at g8-brief; several planning reports are still acquired internally at g8-return-brief. Removing or moving those effects would change logs and prerequisites. They do not authorize early presentation. Locked titles, counters, badges, inline links, tooltips and accessibility text must be absent, not merely a body hidden behind an “unavailable” label.
4. Existing preparation options cite the locked primer and termination rule; automatic return-planning lines cite air/reserve cards before their optional answers. Filter those citation links until their actual unlock. The later recovery-lesson option also cites the air card; selecting that lesson must not retroactively count as asking the earlier question. A skipped optional answer stays unencountered even after the scene and mission end.
5. Evaluate each conditional line at its encounter boundary. Do not scan future nodes or unlock a line from an unplayed branch because its condition happens to hold now. Reconstruct availability on load/replay. Keep notification bookkeeping in presentation state; do not perturb save identity or logs. Existing 0.5.4 saves remain subject to the repository's version rejection policy; no migration is introduced.
6. Announce every actual addition visibly. Use doc 49's quiet counter highlight and “Added to evidence: <title>” / “Added to the binder: <title>” strip; include an equivalent History cue. Group simultaneous arrivals without losing any title, and keep notification text accessible. Counts must count eligible visible records. Trigger at the presentation boundary, not at the old hidden acquisition event. Loading or reopening an overlay should not create duplicate first-arrival announcements.

### History needs a separate consumer audit

The baseline renderHistory prints the complete alternate-history explanation, CAPCOM note and source registry, and gates the prologue note on a run existing. Those paths do not consume evidence availability today. Evidence metadata alone therefore cannot satisfy R2 for History.

- Gate historical material by what was actually presented in the prologue, lines or unlocked pages. Skipping the prologue must not count as seeing its facility note. No whole source-registry dump at mission start.
- A source associated with an early scene may have a title or note revealing a later crisis. Do not reveal its entire note just because the source id was cited; keep unseen historical context hidden until its relevant scene. The H5 note and the five post-flight positions belong after g8-line-postflight-context.
- The current alternate-history explanation mentions specific later branches. Preserve its approved wording in the registry; expose it only once the described material is earned, or implement scoped blocks in the Claude Code integration. Do not use a generic “run exists” condition. Historical-choice lamp behavior and mechanics remain unchanged.
- Apply the same rule to all History entry paths and visibly announce newly available context. Credits/licence strings remain verbatim; this packet does not rewrite legal notices to solve History presentation.

## Copy and licence changes

Binder empty state: **Nothing adopted yet. Reference pages appear as the mission gives you them.** This is the only app/render.ts change.

H5 appended exactly: **Article text checked against the attributed lines on 8 September 2026 (doc 44); all five positions present verbatim.** This records Dan's approved check; the newly displayed context is still the already-authored paraphrase. The sentence is not a claim that its paraphrase is itself quoted verbatim.

Registry credits and site.open_source, and every manifest music/kit licence string, are copied from 423e1ec unchanged. The manifest changes only its content version. The release check passes with MIT code and CC BY 4.0 original content, retaining third-party terms. references/licence-string-changes.md includes old text solely as a historical old/new audit; no old proposed-state string was restored to shipped content.

## Integration and validation

1. Check integration/baseline-hashes.json against 423e1ec. Hashes describe exact Git blob bytes; normalize checkout CRLF to LF or hash git show output. If Claude's checkout has moved on, merge the scoped changes rather than overwrite later work.
2. Apply **integration/content-0.5.5.patch** once, from the repository root. It covers 27 changed/new paths. integration/schema-0.5.5.patch is a review-only subset already included in the main patch; do not apply both. Full changed schemas are under schema/. Supporting source/test snapshots are under integration/files/. Baseline fixtures are retained for reproducible mechanics comparisons, not loaded by the game.
3. Complete R2's runtime/presentation work above. The schema requires exactly one valid unlock form on every record; the validator additionally rejects dangling node/line/preparation targets and duplicate stable line ids. No new media files need copying.
4. Run npm run validate, npm run typecheck, npm test, npm run dialogue-sheet, npm run build and npm run release-check after integration. Regenerate sheets again after Claude's renderer changes. Candidate checks: **299 tests / 19 files; 56 validator routes; six outcomes; 112 follow-on plan commits; zero draws.** The separate 112-route baseline comparison includes questions asked and skipped, and verifies state at every input, logs except the changed identity header, and the follow-on view.
5. The generated dialogue sheet has **861 rows, 225 branch-only, nine existing unreachable strings**. It clears all candidate text against the baseline renderer; its current location/visibility coverage is not proof of R2 behavior. docs/unlock-review-055.* is the timing contract. The nine unreachable rows are existing unavailable-option reasons and reserved future result tiers, not missing evidence presentation.
6. Complete browser acceptance before publishing: fresh start has no cards; every selected drill appears with a cue and every skipped drill stays absent; primer appears at loss of contact; rule and stabilization appear with Mara; each optional card appears only after its own answer; no-prep and paired-prep readbacks behave correctly; pickup and post-flight cards arrive in their scenes; only the selected standing procedure and, on the ground-accountability branch, commissioned task appear; all corresponding History paths stay spoiler-free; save/load reconstructs the same visibility without new domain events or duplicate cues.

Review evidence is in VALIDATION.md, mechanics-audit.json and docs/test-results-055.json. FILES.sha256 covers each packet file except itself; the adjacent ZIP hash and verification record cover the archive. No browser R2 pass or live deployment is claimed in this content drop.
