# FNO-M00c — Playtest-2 presentation fixes

**Task id:** FNO-M00c · **From:** Claude (Cowork) for Dan · **To:** Claude Code · **Date:** 7 September 2026
**Repo:** `C:\Users\wolfe\projects\failure-is-not-an-option`, `main`, from `223a6cb` (M00b delivery). Content stays **0.5.1**; no content, schema or engine change in this pass.
**Shared folder:** `C:\Users\wolfe\Documents\Claude_GPT_Shared_Workflow\Failure-is-Not-an-Option` = `SHARED\`.
**Deliverable:** commits on `main` + `SHARED\FNO-M00c-BUILD.zip` (same shape as M00b's: `00_BUILD.md`, evidence, the affected screenshots, regenerated sheet). Nothing else written into `SHARED\`.

Source: `docs/26-Playtest-2-Findings.md` — Dan's notes verbatim in §1, the rulings that change earlier direction in §2. The numbers below are Dan's note numbers. All standing rules from the M00b handoff still apply (no fiction call-outs in play text, presentation never touches the simulation, label never delete, no NASA marks).

## Changes

1. **Sound on by default (note 1).** After Begin (or Skip), the master bus is on unless the player has previously turned it off (`fno.audio` persisted setting wins). The SOUND key and volumes are unchanged. Update the e2e "audio off by default" test to "silent until Begin, on after Begin, persisted off stays off"; the "no audio in the log / replay byte-identical" tests stay.
2. **One continuous opening scroll (notes 2, 3, 4).** Dedication and notices become one column with a chapter gap (≈ 1.5 line-heights of empty space) between them; no Continue between them; the scroll box's scrollbar is hidden (`scrollbar-width: none` / `::-webkit-scrollbar { display: none }`) while manual wheel/keyboard scrolling still works when paused. When the last line has cleared the top: 1.2 s hold → fade to black (1.0 s) → hero title fades in (1.5 s). Continue at any time: quick fade (0.4 s) to the hero title. Pause/Resume, Skip to menu and Sound stay outside the text. Reduced motion: two static pages (dedication, notices) with Continue and immediate cuts, as now. The montage slot stays where it is (between notices and title).
3. **Hero title 25 % smaller (note 5).** Multiply the study-`a` font sizes in `title-layout.json` by 0.75 (FAILURE 240.6 px, IS NOT AN 202.05 px, OPTION 270.6 px); keep the left edge at x 475 and scale the baselines about the block's vertical centre so the block stays centred where it was. The menu lockup keeps its 0.826 ratio to the hero. Re-check Glen's head clear zone and the Continue label (≥ 18 px at 1366×768).
4. **Full-screen line (note 6).** Under the menu keys, one line in the subtitle style: "Best played full screen — press F11 on Windows." Add a FULL SCREEN key (kit selector face) that calls `document.documentElement.requestFullscreen()`; hide the key when the API is unavailable or refuses; label flips to EXIT FULL SCREEN while active. Keyboard-reachable.
5. **Crisis cue → Drum Background (note 9).** Copy `SHARED\audio\Mission in Danger Drum Background.mp3` to `public/audio/`, manifest entry `kind: audio` (title, credit and rights from `docs/audio-README.md`; duration 126.9 s). In `app/music-map.json`, the `crisis` cue's asset becomes `audio-mission-in-danger-drum-background` with a new per-cue `gain: 0.75`; start 0.0, once, fade 2 s on the return Choose, still `provisional`. *Mission in Danger* keeps its manifest entry, uncued (listed in About as part of the OST). Update the duration test and README's cue table.
6. **Emblem beneath the UI layer (note 10).** The emblem overlay is part of the room composite: draw it in the room layer (below every panel, card region and status bar), never above them. Re-assert the emblem placement test on the room layer only.
7. **Details open by default (note 12).** Every card's Details disclosure starts open; the per-card toggle memory stays. Remove the "open at the return fork only" special case.
8. **History panel trim (note 14).** Do not render, in History: the per-item provenance reference lines for reports (evidence), procedures and debrief departures, and the fiction register (F-labels) anywhere. Keep: the lamp explanation sentence, the departure/history explanation text (`ui.text` row 463 in the sheet), and the sources list H1–H8. The data is untouched; the dialogue sheet keeps the rows (mark them `history-hidden` in the sheet's kind column if the generator needs a flag to keep the reachability proof honest — document what you did); README's rights/sources sections unchanged.
9. **Conversation panel (Claude's observation, confirmed by Dan's screenshot).** No horizontal scrollbar (`overflow-x: hidden`, and fix whatever is wider than the panel — likely the question keys or a portrait row). Size the panel so Glen's question keys are visible without scrolling at both viewports and both text sizes on the return decision and every other question screen; if the panel would exceed its max height, the dialogue scrolls, the questions stay pinned at the bottom of the panel. Show the rehearsal readout once: drop the prompt-line copy (keep the one under "Supported by", which is now always visible with Details open).
10. **Idle highlight and hints infrastructure (note 8, presentation half).** A 30 s idle timer (any input resets it; presentation only, never logged). On expiry: on a non-decision screen, the primary continuation key takes a muted highlight — the kit selected face at reduced opacity, steady (no pulse; nothing animates under reduced motion). On a decision screen, if `hint` text exists on the node (content 0.5.2 will add it; none exists yet), show it in a paper strip above the cards; if none exists, highlight nothing. Settings gets **HINTS: SHOW / HIDE** (default show, persisted `fno.hints`). Add the `hint?: string` optional field to the decision-node type and schema **as a no-op read** (the validator accepts it; no content uses it yet) so 0.5.2 drops in as content-only.

## Checks

`npm run validate` (unchanged numbers, fingerprint `9056d94a…98f6`), `npm test`, `npm run test:e2e` (update the opening-flow, audio-default, History and Details tests; add: hidden scrollbar + continuous scroll, fade sequence timing under fake timers, full-screen key present/absent, emblem below panels, no horizontal overflow in the conversation panel at both viewports/sizes, idle highlight after 30 s with fake timers and its absence under reduced motion, hints toggle persisted), `npm run dialogue-sheet` regenerated and committed, contrast re-measured on the changed screens (menu line and key, hint strip, muted highlight), screenshots re-shot for: opening (three points in the scroll), hero title, menu, return decision, History panel, a decision screen with the idle highlight.

## Do not

No content edits (0.5.2 is Codex's), no prologue (M01), no astronaut portraits (M01), no Modern skin, no engine or save changes, no removal of any content string.
