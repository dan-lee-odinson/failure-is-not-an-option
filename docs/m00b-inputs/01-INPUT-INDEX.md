# FNO-M00b inputs

| Path | What |
|---|---|
| `00_HANDOFF.md` | The task. Read first. |
| `content-0.5.0/`, `content-0.5.1/` | Codex content packets, hashes verified. Apply 0.5.0 adapter, then 0.5.1 patch (against 0.5.0). |
| `music-map.json`, `soundscape-map.json` | Starting cue maps (copy into `app/`). |
| `title-layout.json` | Hero title A coordinates (copy of `SHARED\art\title-hero-v003\title-layout.json`). |
| `audio-README.md`, `audio-analysis.json` | Rights lines and measured track data. |
| `docs/` | Governing docs 07, 08, 12, 16–25. |
| **Not in this zip (large; read from the shared folder):** | `SHARED\art\apollo-navigation-v002\` (kit v002), `SHARED\art\title-hero-v003\` (hero A, fonts), `SHARED\audio\*.mp3` (five tracks), `SHARED\audio\soundscape-cc0\` (Dan's seven downloads are in place; the TRP file is .mp3, the rest .wav). Verify with the `FILES*.sha256` in each folder (CRLF line endings — strip `\r`). |
